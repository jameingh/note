# 4月7日
## 文件透明加密+免改造需求评审

排期

http://10.20.114.45:8090/pages/viewpage.action?pageId=99460703

## ✅ipsec高性能版本前+后端+server端联调

## ✅流水线问题排查和处理

问题：打包失败报错
解决方法：
打包系统重构后有问题，修复后解决

## ✅贵州人社厅vpn现场问题

系统重装失败，根据报错提示，现场的用来安装dasos的启动u盘有问题，重新制作启动·u盘

# 4月8日
## vpn4.2pqc版本测试问题排查和修复
问题描述：
点击"导出许可申请"按钮后台报错

问题分析：
调用的是公共模块的接口。

解决方法：
依赖的公共模块需要升级，升级后解决

问题描述2:缺少pqc so库
解决方法：
在项目中添加pqc so库，重新编译。

## 密盾解密报错20505

问题描述：
用户内部群下载文件失败，但是单聊没问题。

问题分析：
加解密方法是不care单聊、群聊、内部群的。加解密函数只接受消息内容。单聊的内容没有问题，但是内部群的内容有问题。说明内部群的内容格式有问题。

排查过程：
代码中搜索20505，找到相关代码decryptFile_file_headinfo_error_format，
![alt text](midun0408.png)
继续搜索decryptFile_file_headinfo_error_format，找到相关代码parseHeadInfo函数。

AI分析parseHeadInfo函数的实现，了解什么情况下会报20505错误。

```markdown
用户在询问关于 `decryptFile_file_headinfo_error_format` 这个错误在什么情况下会出现。这是一个代码搜索问题，我需要在项目中搜索这个错误字符串来找到相关的上下文。
我来搜索这个错误相关的代码。

找到了相关代码。这个错误定义在 native.h 中，并在 safechat.c 中使用。让我查看 safechat.c 中这个错误的上下文，了解什么情况下会报这个错误。
现在我明白了。让我进一步查看 parseHeadInfo 函数，看看什么情况下它会返回 NULL，从而导致这个错误。
我需要查看parseHeadInfo函数的实现，以了解什么情况下会返回NULL导致这个错误。

现在我清楚了。这个错误是在 `parseHeadInfo` 函数返回 NULL 时产生的。让我总结一下会触发这个错误的几种情况。



根据代码分析，`decryptFile_file_headinfo_error_format` 错误会在以下情况触发：

## 触发条件

`parseHeadInfo()` 函数返回 `NULL` 时会报此错误，具体有以下几种情况：

1. **内存分配失败** - malloc 分配 `msg_HandInfo_t` 失败

2. **文件大小过小** - 文件大小 ≤ `sizeof(msg_HandInfo_t)`（文件头结构体大小）

3. **文件打开失败** - 无法以二进制模式打开文件

4. **读取文件头失败** - fread 读取文件头数据失败

5. **文件格式错误** - 文件头的 `magic[0]` 和 `magic[1]` 不匹配预设的 `MAGIC_ONE` 和 `MAGIC_TWO`（魔数校验失败）

6. **参数错误** - 同时传入 `filecontent` 和 `sourcefile` 都为 NULL/空

其中最常见的是第5种情况：**文件格式不合法**，即被解密的文件不是正确的加密文件格式（魔数验证失败）。
```

## 现场管理员用户的ukey丢了，如何解绑和重新绑定新ukey

问题描述：
现场管理员用户的ukey丢了，如何解绑和重新绑定新ukey。

问题分析：
在后台清除旧ukey和用户绑定关系

http://10.20.114.45:8090/pages/viewpage.action?pageId=61132288

# 4月9日

# 4月10日

## Ipsec高性能版本隧道建立失败问题排查

问题原因：
相同子网的协商算法应该一致，配置错误

## ipsec高性能版本重载配置bug排查和修复

问题原因：
重载配置的时候，重启了ipsec，应该判断是否需要重启ipsec服务

## ipsec高性能版本策略问题排查和修复

问题原因：
策略默认值是enable，用户在更新其他字段时，enable 被意外重置为 true，造成策略被错误地启用。

问题分析：
如果保留默认值，会导致用户在更新其他字段时，enable 被意外重置为 true，造成策略被错误地启用。

## ipsec高性能版本修复子线程查询数据一致性问题

问题原因：
在子线程中查询主线程未提交事务的数据，会导致数据不一致。

解决方法：
修复子线程查询数据一致性问题，先更新子网启用/不启用状态，再查询出子网列表，传入子线程。保证子线程获取的子网数据和主线程一致

## 抗量子产品名称修改

# 4月13日

## 内蒙古项目安全测试问题沟通、排查、修复方案讨论

和vpn相关的有多个nginx问题和一个mysql数据库问题

mysql数据库是双主部署，吴颖推荐开启防火墙的白名单，3306只对两个mysql服务器自身开放。从而避免扫描出问题。

mysql一直在更新，bug过一段时间还会有，不建议直接升级到最新版本。

查看进程监听的端口
```bash
# 查出mysql的进程号是1239
ps -ef|grep mysql
# 根据进程号查询监听端口
netstat -tulnp | grep 1239
```

10.50.91.132上的nginx配置，不包含不安全的TLS12_RSA_WITH_AES_128_CBC_SHA套件

```bash
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:AES128-SHA:DES-CBC3-SHA:ECC-SM4-SM3:ECDHE-SM4-SM3:SM2-WITH-SMS4-SM3:ECDHE-SM2-WITH-SMS4-GCM-SM3;
```

cd /usr/local/nginx/conf/conf.d/
vi sslvpn-manager-nginx.conf
```bash
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers 'ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:!RSA:!EXPORT:!aNULL';
ssl_prefer_server_ciphers on;
```
/usr/local/nginx/sbin/nginx -s reload

```bash
systemctl start firewalld
remote_host=10.50.91.132
firewall-cmd --permanent --add-rich-rule="rule family='ipv4' source address='$remote_host' port protocol='tcp' port='3306' accept"
firewall-cmd --add-port=8443/tcp --permanent
firewall-cmd --reload
```

## Q2季度绩效制定
> 明天要发给领导

中电信后量子vpn开发适配

ipsec vpnv4.0版本迭代开发和部署到上海现场

ipsec vpnv4.1小版本迭代开发

密码应用安全管理平台v1.0版本设计和开发发版

透明加密免改造系统支持协助开发

vpn历史版本维护、安全问题持续修复

# 4月14日

## 内蒙古项目安全测试问题沟通、排查、修复方案讨论

去现场修复安全问题，数据库双主部署，防火墙用的iptables，原iptables策略配置了多个网段允许3306端口，导致扫描出问题。

应该精确到只允许特定IP访问3306端口。

现场10.40.10.71是主机，10.40.10.72是备机

```bash
iptables -D INPUT -s 10.9.0.0/16 -p tcp -m tcp --dport 3306 -j ACCEPT
iptables -D INPUT -s 192.168.2.0/24 -p tcp -m tcp --dport 3306 -j ACCEPT
iptables -D INPUT -s 192.168.4.0/24 -p tcp -m tcp --dport 3306 -j ACCEPT
iptables -D INPUT -s 10.14.10.0/24 -p tcp -m tcp --dport 3306 -j ACCEPT


iptables -I INPUT -s 10.14.10.172 -p tcp --dport 3306 -j ACCEPT
iptables -I INPUT -s 10.14.10.171 -p tcp --dport 3306 -j ACCEPT

service iptables save

iptables -L INPUT -n --line-numbers
# 如果根据行号删除规则只能一个个从大的行号开始删除，不能从小的行号开始删除


mysql -uroot -p
Hzflk@2022!
# 查看实际主ip
show slave status\G;
```
```bash
service iptables save
```
/etc/sysconfig/iptables

## 安恒密盾某客户提出图片延迟的问题处理讨论