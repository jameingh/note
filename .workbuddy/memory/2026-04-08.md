# 2026-04-08 日记

## 将 backend-patterns SKILL.md 转化为 Java/Spring Boot 技术栈

- 来源：https://github.com/affaan-m/everything-claude-code/blob/main/skills/backend-patterns/SKILL.md
- 输出位置：`~/.workbuddy/skills/backend-patterns-java/SKILL.md`（用户已复制到 `/Users/akm/Documents/note/skills/backend-patterns-java/SKILL.md`）
- 技术栈转换：Node.js / Express / Next.js → Java 17 / Spring Boot 3.1.1 / Maven / MyBatis-Plus 3.5.x
- 覆盖章节：
  - pom.xml 核心依赖
  - RESTful API 设计（Controller + 统一 R 包装）
  - 仓库层（MyBatis-Plus BaseMapper、QueryWrapper）
  - 服务层（ServiceImpl extends ServiceImpl）
  - 中间件（HandlerInterceptor、OncePerRequestFilter）
  - 数据库模式（N+1 预防、分页插件、事务）
  - 缓存（RedisTemplate 手动缓存、@Cacheable/@CacheEvict 注解缓存）
  - 错误处理（BizException + ErrorCode + @RestControllerAdvice）
  - 指数退避重试（Spring Retry + @Retryable）
  - 认证授权（JwtUtil + UserContext ThreadLocal + RBAC AOP切面）
  - 限流（Redis ZSet 滑动窗口 + RateLimitInterceptor）
  - 异步任务（@Async + @Scheduled + Spring Events）
  - 日志（@Slf4j + MDC + logback-spring.xml JSON输出）
  - 参数校验（Bean Validation + @Valid）
