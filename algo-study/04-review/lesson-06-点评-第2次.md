# Lesson 06 点评（第 2 次）

## 结论

`_resize` 修正通过，Lesson 06 正式完成。

## 通过点

1. 扩容后使用 `new_data` 重建底层存储，`len(data)` 与 `capacity` 一致。
2. 只拷贝前 `size` 个有效元素，逻辑正确。
3. `append`、`get`、`insert` 主流程与边界检查都可用。

## 小建议

后续可以在 `append` 的复杂度理由里再补一句：
“单次扩容会慢，但长期平均追加依然很快（均摊意义上接近常数）。”

## 进度

第二阶段已开始，下一课进入 Lesson 07（链表原理与实现）。
